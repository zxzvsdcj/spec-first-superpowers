# 使用说明

## 1. 系统目标

强制 AI 编程助手先完成规范（SDD）再写代码，杜绝"凭感觉乱写"。
集成五个核心工具：Spec-Kit + OpenSpec (OPSX) + Superpowers + planning-with-files + ui-ux-pro-max (v2.0)。

**v4 核心升级**：OPSX 标准化工作流 · 自动化审查循环 · 文件结构映射 · 子代理模型选择 · ui-ux-pro-max v2.0 · 范围检查。

## 2. 安装

### 一键安装（推荐）

```bash
npx skills add zxzvsdcj/spec-first-superpowers
```

无交互安装（适合脚本或批量交付）：

```bash
npx skills add zxzvsdcj/spec-first-superpowers --skill spec-first-superpowers --agent cursor --global --yes
```

### 外部 CLI（按需）

```bash
chmod +x install-all.sh && ./install-all.sh
```

```powershell
.\install-all.ps1
```

### Cursor Skills 依赖

在 Cursor 中确保已安装：
- `using-superpowers`（含子 Skills）
- `planning-with-files`
- `finishing-a-development-branch`
- `ui-ux-pro-max`（推荐，v2.0）

### 可选：守门规则

将 `.cursor/00-spec-first-superpowers.mdc` 复制到项目的 `.cursor/rules/` 目录，确保 AI 始终优先触发规范流程。

## 3. 使用方式

### 启动

```
/super-spec
```

或带参数：

```
/super-spec force-openspec 添加用户仪表盘
```

### 完整流程

1. **复杂度分级** — AI 自动判断快速/标准/深度，用户确认
2. **上下文恢复** — 检测已有 task_plan.md 时自动恢复（5 问重启测试）
3. **SDD 规范** — 按模式执行：OpenSpec 用 `/opsx:propose` 或 `/opsx:explore`；Spec-Kit 用 `/speckit.specify`
4. **闸门 G1** — 用户确认 + 宪法对齐 + **Spec 审查循环**（subagent 自动审查，最多 3 轮）
5. **持久规划** — 生成三文件 + **文件结构映射** + **Plan 审查循环**，闸门 G2
6. **UI/UX 设计** — 条件触发，v2.0 智能设计系统生成（67 styles, 161 palettes, 57 fonts, 13 stacks），`--persist` 持久化，闸门 G3
7. **TDD 执行** — Subagent-Driven（含**模型选择** + **实现者状态处理**）或 Executing-Plans，闸门 G4
8. **归档** — finishing-a-development-branch + 更新全部文件

### v4 新增能力

| 能力 | 说明 |
|------|------|
| `/opsx:propose` | 一步创建变更 + 生成全部规划工件（OpenSpec 默认快速路径） |
| `/opsx:explore` | 探索想法、调查问题（无结构化要求） |
| `/opsx:verify` | 三维度验证：完整性 × 正确性 × 一致性（扩展配置文件） |
| `/speckit.implement` | 按计划执行所有任务 |
| `/speckit.analyze` | 跨工件一致性与覆盖率分析 |
| `/speckit.checklist` | 生成自定义质量清单 |
| Spec 审查循环 | brainstorming 自动分派 reviewer subagent（G1） |
| Plan 审查循环 | writing-plans 自动分派 reviewer subagent（G2） |
| 文件结构映射 | 任务分解前先映射所有文件职责 |
| 模型选择 | 按任务角色选择最低成本模型（机械→fast, 集成→标准, 架构→capable） |
| 实现者状态 | DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED 四种处理路径 |
| 范围检查 | 早期检测多子系统需求并标记分解 |

## 4. 常见问题

| 问题 | 解决 |
|------|------|
| 未触发？ | 检查 skills 是否已安装，重启 Cursor |
| 模式错误？ | `/super-spec reset` 重置 |
| UI 未触发？ | 需求中明确写界面关键词 |
| 上下文丢失？ | 检查 task_plan.md / progress.md 是否最新 |
| 3 次错误？ | 自动调用 systematic-debugging |
| OpenSpec 目录未找到？ | OpenSpec 现使用 `openspec/`（非 `.openspec/`），运行 `openspec init` |

## 5. 详细参考

- [质量闸门标准](skills/spec-first-superpowers/references/quality-gates.md)
- [协同模式](skills/spec-first-superpowers/references/synergy-patterns.md)
- [集成指南](skills/spec-first-superpowers/references/integration-guide.md)
- [OpenSpec 工作流](skills/spec-first-superpowers/references/openspec-workflow.md)
- [Spec-Kit 工作流](skills/spec-first-superpowers/references/spec-kit-workflow.md)
