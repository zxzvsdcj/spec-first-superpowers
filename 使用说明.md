# 使用说明

## 1. 系统目标

强制 AI 编程助手先完成规范（SDD）再写代码，杜绝"凭感觉乱写"。
集成五个核心工具：Spec-Kit + OpenSpec + Superpowers + planning-with-files + ui-ux-pro-max。

## 2. 安装

### 外部 CLI

```bash
chmod +x install-all.sh && ./install-all.sh
```

### Cursor Skills

在 Cursor 中确保已安装：
- `using-superpowers`（含子 Skills）
- `planning-with-files`
- `ui-ux-pro-max`（推荐）

### 文件放置

| 文件 | 放置位置 |
|------|----------|
| `SKILL.md` + `references/` + `assets/` | `~/.cursor/skills/spec-first-superpowers/` |
| `00-spec-first-superpowers.mdc` | `<project>/.cursor/rules/` |
| 宪法模板（可选） | `.specify/memory/` 或 `.openspec/` |

## 3. 使用方式

### 启动

在 Cursor 聊天中输入：

```
/super-spec
```

或带参数：

```
/super-spec force-openspec 添加用户仪表盘
```

### 完整流程

1. **模式选择** — 自动判断 Spec-Kit 或 OpenSpec（默认 OpenSpec）
2. **SDD 规范** — 按模式执行规范流程，用户确认后继续
3. **持久规划** — 生成 task_plan.md / findings.md / progress.md
4. **UI/UX 设计** — 涉及界面时自动触发设计系统生成
5. **TDD 执行** — Superpowers 方法论：测试→实现→审查→验证
6. **归档** — 更新文件状态

### 典型示例

```
你：/super-spec 新增登录页面，支持邮箱+密码和 Google 登录
AI：[自动选择 OpenSpec] → 规范流程 → 设计系统 → TDD 实现
```

## 4. 常见问题

| 问题 | 解决 |
|------|------|
| 未触发？ | 检查 rules/skills 路径是否正确，重启 Cursor |
| 模式错误？ | `/super-spec reset` 重置 |
| UI 未触发？ | 需求中明确写界面相关关键词 |
| 上下文丢失？ | 检查 task_plan.md / progress.md 是否最新 |

## 5. 文件结构说明

详见 [README.md](README.md) 的项目结构章节和 [references/integration-guide.md](references/integration-guide.md)。
