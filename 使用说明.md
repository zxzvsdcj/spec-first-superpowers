# 使用说明

## 1. 系统目标

强制 AI 编程助手先完成规范（SDD）再写代码，杜绝"凭感觉乱写"。
集成六个核心工具：Spec-Kit + OpenSpec (OPSX) + Superpowers + planning-with-files + ui-ux-pro-max (v2.5.0) + MemPalace。

**v5 核心升级**：Inline Self-Review 替代 subagent 审查循环 · MemPalace 跨会话记忆集成 · Spec-Kit Workflow Engine · ui-ux-pro-max v2.5.0 · OpenSpec `/opsx:refine` · 全版本同步。

## 2. 安装

### 一键安装（推荐）

```bash
npx skills add zxzvsdcj/spec-first-superpowers
```

无交互安装（适合脚本或批量交付）：

```bash
npx skills add zxzvsdcj/spec-first-superpowers --skill spec-first-superpowers --agent cursor --global --yes
```

### 外部 CLI（按需）

```bash
chmod +x install-all.sh && ./install-all.sh
```

```powershell
.\install-all.ps1
```

### Cursor Skills 依赖

在 Cursor 中确保已安装：
- `using-superpowers`（含子 Skills）
- `planning-with-files`
- `finishing-a-development-branch`
- `ui-ux-pro-max`（推荐，v2.5.0）

### 可选：MemPalace

```bash
pip install mempalace
mempalace init ~/projects/your-project
```

在 Cursor Settings → MCP 中添加 mempalace 服务器。详见 `references/mempalace-integration.md`。

### 可选：守门规则

将 `.cursor/00-spec-first-superpowers.mdc` 复制到项目的 `.cursor/rules/` 目录，确保 AI 始终优先触发规范流程。

## 3. 使用方式

### 启动

```
/super-spec
```

或带参数：

```
/super-spec force-openspec 添加用户仪表盘
```

一键升级（自动检查所有集成项目的最新版本并执行升级）：

```
/super-spec upgrade
```

### 完整流程

1. **复杂度分级** — AI 自动判断快速/标准/深度，用户确认
2. **上下文恢复** — 检测已有 task_plan.md 时自动恢复（5 问重启测试 + MemPalace 历史查询）
3. **SDD 规范** — 按模式执行：OpenSpec 用 `/opsx:propose` 或 `/opsx:explore`；Spec-Kit 用 `/speckit.specify`
4. **闸门 G1** — 用户确认 + 宪法对齐 + **Inline Spec Review**（自检清单，~30秒/轮）
5. **持久规划** — 生成三文件 + **文件结构映射** + **Inline Plan Review**，闸门 G2
6. **UI/UX 设计** — 条件触发，v2.5.0 智能设计系统生成（67 styles, 161 palettes, 57 fonts, 14 stacks, 6 specialist skills），`--persist` 持久化，闸门 G3
7. **TDD 执行** — Subagent-Driven（含**模型选择** + **实现者状态处理**）或 Executing-Plans，闸门 G4
8. **归档** — finishing-a-development-branch + 更新全部文件 + MemPalace 决策归档

### v5 新增能力

| 能力 | 说明 |
|------|------|
| Inline Self-Review | 替代 subagent 审查循环，~30秒/轮（vs ~25分钟），质量可比 |
| MemPalace 集成 | 跨会话记忆 · Knowledge Graph 决策追踪 · Agent Diary 审计 · 跨项目 Spec 模式发现 |
| Spec-Kit Workflow Engine | 自定义工作流注册与发现（Catalog 系统，v0.7.0+） |
| `/opsx:refine` | 针对性审查和优化已有 spec 工件（custom profile） |
| OpenSpec 术语更新 | "expanded" → "custom" profile（v1.2.0） |
| ui-ux-pro-max v2.5.0 | +6 新 Skills（banner-design/slides/ui-styling/design-system/design/brand）· Three.js · 1923 Google Fonts |
| 6 条协同链 | 新增 Chain 6：MemPalace Memory Chain |

### 从 v4 保留的能力

| 能力 | 说明 |
|------|------|
| 复杂度分级 | 快速/标准/深度三级 |
| 会话恢复 | 5 问重启测试 |
| 质量闸门 G0-G4 | 含宪法验证 |
| 两阶段审查 | Spec 合规 + 代码质量 |
| Subagent 执行 | 零上下文污染 |
| 模型选择 | fast → standard → capable |
| 实现者状态 | DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED |
| 范围检查 | 早期分解检测 |
| 文件结构映射 | 任务分解前映射 |
| 设计持久化 | `--persist` 跨会话复用 |

## 4. 常见问题

| 问题 | 解决 |
|------|------|
| 未触发？ | 检查 skills 是否已安装，重启 Cursor |
| 模式错误？ | `/super-spec reset` 重置 |
| UI 未触发？ | 需求中明确写界面关键词 |
| 上下文丢失？ | 检查 task_plan.md / progress.md 是否最新；启用 MemPalace |
| 3 次错误？ | 自动调用 systematic-debugging |
| OpenSpec 目录未找到？ | OpenSpec 现使用 `openspec/`（非 `.openspec/`），运行 `openspec init` |
| Profile 术语变了？ | v1.2.0 将 "expanded" 改名为 "custom"，运行 `openspec config profile` |
| Spec-Kit --ai 不工作？ | v0.7.1 已废弃 `--ai`，改用 `--integration` |

## 5. 详细参考

- [质量闸门标准](skills/spec-first-superpowers/references/quality-gates.md)
- [协同模式](skills/spec-first-superpowers/references/synergy-patterns.md)
- [集成指南](skills/spec-first-superpowers/references/integration-guide.md)
- [OpenSpec 工作流](skills/spec-first-superpowers/references/openspec-workflow.md)
- [Spec-Kit 工作流](skills/spec-first-superpowers/references/spec-kit-workflow.md)
- [MemPalace 集成](skills/spec-first-superpowers/references/mempalace-integration.md)
- [升级协议](skills/spec-first-superpowers/references/upgrade-protocol.md)
